_schema-version: "3.2"
ID: estib
version: 0.0.1
modules:
- name: estib-approuter
  type: approuter.nodejs
  path: estib-approuter
  requires:
  - name: estib_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: estib_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: estib_html_repo_host
    parameters:
      content-target: true
resources:
- name: estib_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: estib_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
